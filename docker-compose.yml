services:
  swag:
    build: .
    ports:
      - "8765:8000"
    volumes:
      - ./swag:/app/swag
      - ./data:/app/data:ro
    command:
      - uv
      - run
      - uvicorn
      - swag.app.asgi:app
      - --reload
      - --host
      - 0.0.0.0
      - --port
      - "8000"

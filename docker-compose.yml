services:
  swag:
    build: .
    ports:
      - "8000:8000"
    environment:
      SWAG_TRANSPORT: http
      SWAG_HOST: 0.0.0.0
      SWAG_PORT: 8000
    command:
      - uv
      - run
      - uvicorn
      - swag.asgi:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
